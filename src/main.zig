const std = @import("std");

pub fn main() !void {
    std.debug.print("WHATWG WebIDL Runtime Library v0.1.0\n", .{});
    std.debug.print("This is a library - import as a module in your Zig projects.\n", .{});
    std.debug.print("See README.md for usage examples.\n", .{});
}
